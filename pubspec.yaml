name: catalogo_productos
description: "Catálogo de Productos - Actividad Integradora 1 (Mi Primera Aplicación en Flutter)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Paquetes externos instalados.
  google_fonts: ^8.1.0
  url_launcher: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
